import 'package:flutter/material.dart';

class PublicationsContent extends StatelessWidget {
  const PublicationsContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Publications'));
  }
}