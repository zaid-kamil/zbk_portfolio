import 'package:flutter/material.dart';

class WebAppContent extends StatelessWidget {
  const WebAppContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Web Projects'));
  }
}