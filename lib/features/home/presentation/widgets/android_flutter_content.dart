import 'package:flutter/material.dart';
class AndroidFlutterContent extends StatelessWidget {
  const AndroidFlutterContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Android & Flutter Projects'));
  }
}