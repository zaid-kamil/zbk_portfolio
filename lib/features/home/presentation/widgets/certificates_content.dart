import 'package:flutter/material.dart';

class CertificatesContent extends StatelessWidget {
  const CertificatesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Certificates'));
  }
}