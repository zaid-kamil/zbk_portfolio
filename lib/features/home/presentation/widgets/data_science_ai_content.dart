import 'package:flutter/material.dart';

class DataScienceAiContent extends StatelessWidget {
  const DataScienceAiContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Data Science & AI Projects'));
  }
}